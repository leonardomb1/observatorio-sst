SELECT ano, cid10_codigo, max(cid10_descricao) AS cid10_descricao,        COUNT(*) AS acidentes, SUM(obito) AS obitos FROM {{ source('silver', 'acidentes') }} WHERE ano IS NOT NULL AND cid10_descricao IS NOT NULL GROUP BY 1,2
